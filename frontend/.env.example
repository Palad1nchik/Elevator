# Base URL for API calls from the browser.
# Leave as /api: in production the Go server serves the site and the API from
# the same origin, and `npm run dev` proxies /api to the backend.
VITE_API_BASE=/api

# Used only by `npm run dev` to proxy /api to the local Go backend.
VITE_API_PROXY=http://localhost:8080
